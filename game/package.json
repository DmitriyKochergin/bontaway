{
  "name": "bontaway",
  "homepage": "https://DmitriyKochergin.github.io/bontaway",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "rsbuild dev --open",
    "start": "rsbuild dev --open",
    "clean": "rimraf node_modules build",
    "build": "rsbuild build",
    "lint": "npx @biomejs/biome lint .",
    "lint-fix": "npx @biomejs/biome lint --write .",
    "lint-fix-unsafe": "npx @biomejs/biome lint --write --unsafe .",
    "format": "npx @biomejs/biome format --write .",
    "check": "npx @biomejs/biome check --write .",
    "lint:staged": "npx @biomejs/biome check --error-on-warnings --no-errors-on-unmatched --staged .",
    "predeploy": "rsbuild build",
    "deploy": "gh-pages -d build"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.16",
    "@rsbuild/core": "^2.0.9",
    "@rsbuild/plugin-type-check": "^1.3.5",
    "@types/node": "^25.9.1",
    "gh-pages": "^6.3.0",
    "rimraf": "^6.1.3",
    "typescript": "^6.0.3"
  },
  "packageManager": "yarn@4.14.1",
  "dependencies": {
    "phaser": "^3.83.0",
    "phaser-raycaster": "0.11.0"
  }
}
